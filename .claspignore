# clasp pousse depuis la racine (rootDir "."). On ignore tout, puis on autorise
# uniquement les fichiers deployables de la racine : aucun test/doc/script/config
# n'est envoye dans Apps Script.
**/**
!appsscript.json
!*.gs
!*.html
